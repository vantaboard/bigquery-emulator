name: order_by_1m
tags: [duckdb, sort, heavy]
setup:
  - sql: |
      CREATE TABLE {{ds}}.t AS
      SELECT id, MOD(id * 1000003, 1000000007) AS k
      FROM UNNEST(GENERATE_ARRAY(1, 1000000)) AS id
query: SELECT id, k FROM {{ds}}.t ORDER BY k, id LIMIT 1000
iterations: 5
warmup: 1
max_ms: 120000
