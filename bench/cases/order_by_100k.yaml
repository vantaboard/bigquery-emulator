name: order_by_100k
tags: [duckdb, sort]
setup:
  - sql: |
      CREATE TABLE {{ds}}.t AS
      SELECT id, MOD(id, 10000) AS k FROM UNNEST(GENERATE_ARRAY(1, 100000)) AS id
query: SELECT id, k FROM {{ds}}.t ORDER BY k, id LIMIT 1000
iterations: 10
warmup: 2
