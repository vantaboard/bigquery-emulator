# cast_format_parse_date: CAST(string AS DATE FORMAT '...').
name: cast_format_parse_date
description: |
  CAST(STRING AS DATE FORMAT) parses via googlesql CastStringToDate on
  the semantic executor route promoted by ResolvedCast(extended).
project_id: proj-scalar-cast-format-parse
query: |
  SELECT CAST('2018-12-03' AS DATE FORMAT 'YYYY-MM-DD') AS parsed
expected:
  route: semantic_executor
  rows:
    - {parsed: "2018-12-03"}
