# Source: GoogleCloudPlatform/bigquery-utils @ 0754ad891dea3afe769f9fd5e537a7ea3c8b3a3b
#   udfs/community/linear_interpolate.sqlx (+ test_cases.js)
# License: Apache-2.0. Generated by scripts/sync_bigquery_utils_udfs.sh; do not edit by hand.
name: bqutils_community_linear_interpolate
description: bigquery-utils community UDF linear_interpolate (2 cases)
profiles:
  - duckdb
project_id: proj-bqutils-linear-interpolate
setup:
  - sql: |-
      /*
       * Copyright 2020 Google LLC
       *
       * Licensed under the Apache License, Version 2.0 (the "License");
       * you may not use this file except in compliance with the License.
       * You may obtain a copy of the License at
       *
       *     http://www.apache.org/licenses/LICENSE-2.0
       *
       * Unless required by applicable law or agreed to in writing, software
       * distributed under the License is distributed on an "AS IS" BASIS,
       * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
       * See the License for the specific language governing permissions and
       * limitations under the License.
       */

      -- linear_interpolate:
      -- Input:
      -- pos: the position on x axis for the independent variable
      -- prev: the x,y coordinate of the preceding value
      -- next: the x,y coordinate of the following value
      -- Output: the interpolated y value
      CREATE FUNCTION linear_interpolate(pos INT64, prev STRUCT<x INT64,y FLOAT64>, next STRUCT<x INT64,y FLOAT64>)
      RETURNS FLOAT64 AS (
        CASE
          WHEN pos IS NULL OR prev IS NULL OR next IS NULL THEN NULL
          ELSE
            (next.y - prev.y) / (next.x - prev.x) * (pos - prev.x) + prev.y
        END
      );
query: |
  WITH cases AS (
    SELECT 0 AS case_id, TO_JSON_STRING(linear_interpolate(CAST(2 AS INT64), STRUCT(CAST(1 AS INT64) AS x, CAST(1.0 AS FLOAT64) AS y), STRUCT(CAST(3 AS INT64) AS x, CAST(3.0 AS FLOAT64) AS y))) AS actual, TO_JSON_STRING(CAST(2.0 AS FLOAT64)) AS expected
    UNION ALL
    SELECT 1 AS case_id, TO_JSON_STRING(linear_interpolate(CAST(3 AS INT64), STRUCT(CAST(1 AS INT64) AS x, CAST(1.0 AS FLOAT64) AS y), STRUCT(CAST(4 AS INT64) AS x, CAST(4.0 AS FLOAT64) AS y))) AS actual, TO_JSON_STRING(CAST(3.0 AS FLOAT64)) AS expected
  )
  SELECT case_id, IFNULL(actual = expected, actual IS NULL AND expected IS NULL) AS matches FROM cases ORDER BY case_id
expected:
  match: ordered
  rows:
    - case_id: "0"
      matches: true
    - case_id: "1"
      matches: true
