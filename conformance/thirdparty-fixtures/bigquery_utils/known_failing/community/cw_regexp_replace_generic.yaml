# Source: GoogleCloudPlatform/bigquery-utils @ 8c6ade77f905f249e9791443a90a32a1222885f8
#   udfs/community/cw_regexp_replace_generic.sqlx (+ test_cases.js)
# License: Apache-2.0. Generated by scripts/sync_bigquery_utils_udfs.sh; do not edit by hand.
name: bqutils_community_cw_regexp_replace_generic
description: bigquery-utils community UDF cw_regexp_replace_generic (5 cases)
profiles:
  - duckdb
project_id: proj-bqutils-cw-regexp-replace-generic
setup:
  - sql: |-
      /*
       * Copyright 2022 Google LLC
       *
       * Licensed under the Apache License, Version 2.0 (the "License");
       * you may not use this file except in compliance with the License.
       * You may obtain a copy of the License at
       *
       *     http://www.apache.org/licenses/LICENSE-2.0
       *
       * Unless required by applicable law or agreed to in writing, software
       * distributed under the License is distributed on an "AS IS" BASIS,
       * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
       * See the License for the specific language governing permissions and
       * limitations under the License.
       */

       /* Generic regexp_replace, which is the 6-args version with regexp_mode already decoded
        *
        * haystack = the string to search
        * regexp = the regular expression
        * replacement = the string to replace the match with
        * offset = the 1-based offset where the search will begin
        * occurrence = the 1-based occurrence of the match to replace - overrides
        *   the 'g' flag
        * mode = the javascript regular expression mode flags
        */
      CREATE FUNCTION cw_regexp_replace_generic(haystack STRING, regexp STRING, replacement STRING, offset INT64, occurrence INT64, mode STRING) RETURNS STRING
      LANGUAGE js AS """
        if (haystack == null || regexp == null || replacement == null || offset == null || occurrence == null || mode == null) return null;
        replacement = replacement.replace('\\\\', '$');
        if (mode.indexOf('g') == -1) {
          mode = mode + 'g';
        }
        let regExp = new RegExp(regexp, mode);
        const relevantString = haystack.substring(offset - 1);
        const prefix = haystack.substr(0, haystack.length - relevantString.length);
        if (occurrence == 0) {
          return prefix + relevantString.replace(regExp, replacement);
        }
        for (let count = 0; count < occurrence - 1; count++) {
          if (regExp.exec(relevantString) === null) {
            break;
          }
        }
        const skipped = prefix + relevantString.substring(0, regExp.lastIndex);
        const remaining = relevantString.substring(regExp.lastIndex);
        regExp = new RegExp(regexp, mode.replace('g', ''));
        return skipped + remaining.replace(regExp, replacement);
      """;
query: |
  WITH cases AS (
    SELECT 0 AS case_id, TO_JSON_STRING(cw_regexp_replace_generic("TestStr123456", "Str", "Gbp", CAST(1 AS INT64), CAST(1 AS INT64), "i")) AS actual, TO_JSON_STRING("TestGbp123456") AS expected
    UNION ALL
    SELECT 1 AS case_id, TO_JSON_STRING(cw_regexp_replace_generic("Abcabc", "a", "x", CAST(1 AS INT64), CAST(0 AS INT64), "i")) AS actual, TO_JSON_STRING("xbcxbc") AS expected
    UNION ALL
    SELECT 2 AS case_id, TO_JSON_STRING(cw_regexp_replace_generic("Abcabc", "a", "x", CAST(2 AS INT64), CAST(0 AS INT64), "i")) AS actual, TO_JSON_STRING("Abcxbc") AS expected
    UNION ALL
    SELECT 3 AS case_id, TO_JSON_STRING(cw_regexp_replace_generic("Abcabca", "a", "x", CAST(2 AS INT64), CAST(1 AS INT64), "i")) AS actual, TO_JSON_STRING("Abcxbca") AS expected
    UNION ALL
    SELECT 4 AS case_id, TO_JSON_STRING(cw_regexp_replace_generic("all vowels replaced", "a|e|i|o|u", "X", CAST(1 AS INT64), CAST(0 AS INT64), "i")) AS actual, TO_JSON_STRING("Xll vXwXls rXplXcXd") AS expected
  )
  SELECT case_id, IFNULL(actual = expected, actual IS NULL AND expected IS NULL) AS matches FROM cases ORDER BY case_id
expected:
  match: ordered
  rows:
    - case_id: "0"
      matches: true
    - case_id: "1"
      matches: true
    - case_id: "2"
      matches: true
    - case_id: "3"
      matches: true
    - case_id: "4"
      matches: true
