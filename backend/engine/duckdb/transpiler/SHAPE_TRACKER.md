# Transpiler Shape Tracker (route-aware)

Tracks the **routing disposition** of the GoogleSQL `ResolvedAST` for
the local execution coordinator. The shape names come from
`googlesql/resolved_ast/resolved_ast.h` (generated by
`gen_resolved_ast.py` upstream). The file lives under
`backend/engine/duckdb/transpiler/` for historical reasons; it is
the source of truth for **every** route, not just the DuckDB fast
path.

The vocabulary matches `docs/ENGINE_POLICY.md`. Each row picks one
of seven route dispositions; the **planned** route is recorded for
rows that have not landed yet so future work has an explicit target
strategy.

| status               | meaning |
|----------------------|---------|
| `duckdb_native`      | Lowers to DuckDB SQL whose semantics match BigQuery exactly. The transpiler's `Emit*` method emits the DuckDB form directly. |
| `duckdb_rewrite`     | Lowers to DuckDB SQL with a deliberate structural rewrite (struct/array shape rewrite, JSON operator mapping, ...). Same execution path as `duckdb_native`; the rewrite lives in the transpiler. |
| `duckdb_udf`         | Lowers to DuckDB SQL that calls a DuckDB UDF/macro registered at engine startup. The UDF body owns the BigQuery-specific behavior. |
| `semantic_executor`  | Runs on the local row/value semantic executor instead of DuckDB SQL evaluation. DuckDB is still used as the row source; the executor owns expression evaluation and error surfaces. |
| `control_op`         | DDL / metadata / catalog op routed through the storage layer. Bypasses query execution entirely. |
| `local_stub`         | Specialized feature family the emulator accepts at the analyzer but evaluates against a deterministic BigQuery-shaped placeholder. The handler lives under `backend/engine/semantic/stubs/` (function-level: `KEYS.*`) or `backend/engine/control/stubs/` (statement-level: `CREATE MODEL`). The route exists so client-library startup probes succeed; downstream calls that depend on real semantics (e.g. `ML.PREDICT` over a stub model, AEAD encryption with a stub keyset) still surface `UNIMPLEMENTED`. `docs/ENGINE_POLICY.md` owns the per-family stub contract. |
| `unsupported`        | Deliberately out of scope locally. Surfaces a BigQuery-shaped `UNIMPLEMENTED` (or `INVALID_ARGUMENT` where appropriate). |

Rows that have already landed list their **current** route. Rows that
have not landed list the **planned** route they will land on, followed
by `(planned)` in the Notes column; the engine returns `UNIMPLEMENTED`
for those shapes until the matching plan ships. The router never
silently retries on a different route; promoting a `(planned)` row
requires the matching `Emit*` (or semantic-executor / control-op
handler) plus the conformance fixtures that exercise it.

When you add (or change) a route disposition, edit the matching row
here in the same commit that touches `transpiler.cc` (or the
semantic-executor / control-op code) so the doc stays honest.

## Statements

| Node                                          | Status              | Notes                                                                                     |
|-----------------------------------------------|---------------------|-------------------------------------------------------------------------------------------|
| `ResolvedQueryStmt`                           | `duckdb_native` (subset) | `transpiler-select-core`: lowers the root statement by emitting `query()` then renaming each physical column through `output_column_list()` on the outermost SELECT. `is_value_table()` queries (`SELECT AS VALUE ...`) promote to `semantic_executor` via `VisitResolvedQueryStmt`; evaluator in `backend/engine/semantic/executor.cc`. Pinned by `conformance/fixtures/scalar/select_as_value_scalar.yaml`. |
| `ResolvedExplainStmt`                         | `unsupported`       | BigQuery `EXPLAIN` semantics differ from DuckDB; UNIMPLEMENTED today.                      |
| `ResolvedCreateTableStmt`                     | `control_op`        | DDL routes through `ControlOpExecutor::RunCreateTable`, mutating `Storage` directly. Pinned by `conformance/fixtures/ddl_create_table_round_trip.yaml`. |
| `ResolvedCreateTableAsSelectStmt`             | `control_op`        | CTAS materializes via per-query DuckDB then persists through `Storage`. Pinned by `conformance/fixtures/fastpath/ctas_unnest_narrow.yaml`. |
| `ResolvedCreateViewStmt`                      | `control_op`        | Views register in `view_registry` and inline at analyze time via `FindProjectView`.                                          |
| `ResolvedCreateMaterializedViewStmt`          | `control_op`        | Full-refresh materialization at creation via `RunCreateMaterializedView` (same storage table + query path as CTAS; no incremental refresh). Pinned by `conformance/fixtures/ddl/materialized_view_query.yaml`. |
| `ResolvedCreateProcedureStmt`                 | `control_op`        | Procedure bodies register in `procedure_registry`; `ExecuteCallStmt` runs SQL bodies via `script_executor.cc`. |
| `ResolvedMultiStmt`                           | `semantic_executor` | Script blocks execute via `ExecuteMultiStmtScript` (DECLARE/SET/CALL/SELECT sequencing). |
| `ResolvedCreateConstantStmt`                  | `semantic_executor` | `DECLARE` script variables via `declare_stmt.cc`. |
| `ResolvedCallStmt`                            | `semantic_executor` | `CALL` dispatches through `ExecuteCallStmt` in `script_executor.cc`. |
| `ResolvedCreateFunctionStmt`                  | `control_op`        | SQL-language scalar UDF registration (including `ANY TYPE` templated parameters) lands via `backend/catalog/udf_registry.cc` + coordinator `ExecuteDdl` with write-through to `DuckDBStorage` (`__bqemu_routines`); user functions shadow built-ins on name collision and rehydrate across engine restarts. Call-time evaluation of registered scalar bodies routes to `semantic_executor` through `EvalSqlUdfBody`. SQL UDAFs register through the same path and evaluate via `EvalSqlUdafBody`. Gateway routine metadata parsing accepts `ANY TYPE` in `gateway/routines/ddl.go`. `LANGUAGE js` follows the `local_stub` registration posture in `docs/ENGINE_POLICY.md` (metadata persisted; call-time evaluation surfaces `UNIMPLEMENTED`). Pinned by `conformance/fixtures/udf/`. |
| `ResolvedCreateTableFunctionStmt`             | `control_op`        | SQL TVFs register in `tvf_registry` with DuckDB-backed persistence; `ResolvedTVFScan` evaluates on `semantic_executor`. |
| `ResolvedCreateModelStmt`                     | `local_stub`        | `specialized-feature-policy`: `CREATE MODEL` is accepted at parse / analyze time and the coordinator pre-dispatches `RESOLVED_CREATE_MODEL_STMT` to `backend/engine/control/stubs/create_model.cc::RunCreateModel`, which returns OK without materializing a model (no model storage exists). A subsequent `ML.PREDICT(<stub-model>)` surfaces `UNIMPLEMENTED` via the `ml.*` row in `functions.yaml` (`unsupported`). |
| `ResolvedDropStmt`                            | `control_op`        | `DROP TABLE` / `DROP VIEW` via `RunDropTable`. Pinned by `conformance/fixtures/ddl_drop_table_then_select.yaml`. |
| `ResolvedDropFunctionStmt`                    | `control_op`        | `DROP FUNCTION` / `DROP PROCEDURE` remove registry entries and delete persisted rows from `DuckDBStorage`; `IF EXISTS` swallows `NOT_FOUND`. Pinned by `conformance/fixtures/udf/drop_function.yaml`. |
| `ResolvedInsertStmt`                          | `semantic_executor` (subset) | `INSERT VALUES` (single + multi-row) and `INSERT ... SELECT` (semantic-routed inner scans via `MaterializeScan`; DuckDB-transpilable inner scans promote to `duckdb_native` / `RunInsertSelect`) land on `backend/engine/semantic/dml/` + DuckDB fast path; `ASSERT_ROWS_MODIFIED` checks affected counts before commit. |
| `ResolvedUpdateStmt`                          | `semantic_executor` (subset) | Scalar `SET col = <expr>`, deep-STRUCT `SET s.a.b = ...`, and `UPDATE ... FROM ...` (with multiple-match error) land on `backend/engine/semantic/dml/dml_executor.cc`; `array_offset_column` DML remains deferred. |
| `ResolvedDeleteStmt`                          | `semantic_executor` (subset) | `DELETE FROM t WHERE <pred>` lands on `backend/engine/semantic/dml/dml_executor.cc` (no-WHERE form clears the table). `DELETE` with `array_offset_column` (lateral UNNEST in the target) remains deferred. |
| `ResolvedMergeStmt`                           | `duckdb_rewrite` (subset) | Easy branches (`WHEN MATCHED` + single `WHEN NOT MATCHED BY TARGET`) lower via DuckDB. Harder matrix (`WHEN NOT MATCHED BY SOURCE`, multi-action sequences) promotes to `semantic_executor` at classify time (`route_classifier_visitor.cc`); evaluator in `backend/engine/semantic/dml/dml_merge.cc`. Pinned by `conformance/fixtures/dml/merge_not_matched_by_source.yaml` and `merge_multi_action.yaml`. |
| `ResolvedAssignmentStmt`                      | `semantic_executor` | `SET var = expr` (and `SET (a,b) = (...)` via `ResolvedArgumentRef`) evaluates on `backend/engine/semantic/script/assignment_stmt.cc` against the script `FrameStack`; pinned by `conformance/fixtures/scripting/declare_set_unnest.yaml` and `set_scalar_round_trip.yaml`. |
| `ResolvedExecuteImmediateStmt`                | `semantic_executor` | Dynamic SQL re-enters the coordinator via `script_execute_immediate.cc` (AnalyzeNext path) and `googlesql::ScriptExecutor` (control-flow scripts); pinned by `conformance/fixtures/scripting/execute_immediate_select.yaml`. |
| `ResolvedAlterTableStmt`                      | `control_op`        | `ALTER TABLE` ADD/DROP/RENAME COLUMN and SET OPTIONS mutate `Storage` via `RunAlterTable` (scan-drop-create rewrite). Pinned by `conformance/fixtures/ddl/alter_add_column_round_trip.yaml`. |
| `ResolvedAnalyzeStmt`                         | `control_op`        | Metadata-only no-op: validates table existence, does not persist optimizer statistics. |
| `ResolvedAuxLoadDataStmt`                     | `control_op`        | `LOAD DATA` local `file://` readers (CSV/JSON/Parquet via DuckDB) through `RunLoadData`; `gs://` surfaces unsupported per `docs/ENGINE_POLICY.md`. Pinned by `conformance/fixtures/ddl/export_load_round_trip.yaml`. |
| `ResolvedAssertStmt`                          | `semantic_executor` | `ASSERT <expr> [AS '<msg>']` runs on `backend/engine/semantic/script/assert_stmt.cc`. Surfaces BigQuery's `invalidQuery` 400 with `Assertion failed: <msg>` when the predicate is FALSE / NULL; OK when TRUE. `docs/ENGINE_POLICY.md`. |
| `ResolvedExportDataStmt`                      | `control_op`        | `EXPORT DATA` local writers (CSV/JSON/Parquet via DuckDB `COPY`) through `RunExportData`; `gs://` URIs surface unsupported. Pinned by `conformance/fixtures/ddl/export_load_round_trip.yaml`. |

## Relational scans

| Node                                          | Status                | Notes                                                                                     |
|-----------------------------------------------|-----------------------|-------------------------------------------------------------------------------------------|
| `ResolvedTableScan`                           | `duckdb_native`       | `SELECT "col" AS "alias", ... FROM "<table>"` — `transpiler-emit-scans` plan.             |
| `ResolvedSingleRowScan`                       | `duckdb_native`       | `transpiler-select-core`: emits `SELECT 1` so the analyzer's "no FROM clause" representation composes as a derived table for the wrapping ProjectScan. |
| `ResolvedProjectScan`                         | `duckdb_native`       | `transpiler-select-core`: `SELECT <projections> FROM (<input>)` -- per-column lookup picks each output column out of `expr_list()` (computed) or references it by name (pass-through from `input_scan`). Join-renamed `__bq_j_*` aliases stay visible to `EmitComputedColumn` / `EmitExpr` so post-join expressions (e.g. `COALESCE(u.id, ...)`) bind against join output columns. No-op layers (`expr_list()` empty AND `column_list()` is a permutation of `input_scan()->column_list()` by column id) elide so the analyzer's always-inserted ProjectScan over a TableScan does not stack a redundant `SELECT * FROM (SELECT * FROM ...)` wrap onto every scan emit. |
| `ResolvedFilterScan`                          | `duckdb_native`       | `SELECT * FROM (<input>) WHERE <expr>` — `transpiler-emit-scans` plan.                     |
| `ResolvedJoinScan`                            | `duckdb_native` (subset) | `transpiler-emit-join-agg`: INNER (+ CROSS) / LEFT / RIGHT / FULL via DuckDB's native join keywords; `JOIN USING(...)` (`has_using`) lowers to DuckDB `USING (...)` (`conformance/fixtures/fastpath/join_using_inner.yaml`). Lateral joins (`is_lateral`) and lateral correlated `parameter_list` reroute to `semantic_executor` (`backend/engine/semantic/outer_row_eval.{h,cc}` + `MaterializeJoinScan`). |
| `ResolvedArrayScan`                           | `duckdb_native` (subset) | `transpiler-struct-unnest`: standalone `UNNEST(<arr>) AS <col>` lowers to DuckDB `SELECT unnest(<arr>) AS "<col>"`. Multi-array `UNNEST(a, b, ...)` (`array_zip_mode`), `WITH OFFSET` (`array_offset_column`), `LEFT JOIN UNNEST` / cross-joined `FROM t, UNNEST(t.arr)` (non-`SingleRowScan` `input_scan` / `join_expr` / `is_outer`) reroute to `semantic_executor` (`array_scan.cc` + `MaterializeArrayScan` + `outer_row_eval`; pinned by `conformance/fixtures/array_struct/`). |
| `ResolvedAggregateScan`                       | `duckdb_native` (subset) | `transpiler-emit-join-agg`: GROUP BY + simple aggregates (`SUM`/`COUNT`/`AVG`/`MIN`/`MAX`/`COUNT(*)`) via `ResolvedAggregateFunctionCall`. `advanced-relational-routing`: GROUPING SETS / ROLLUP / CUBE lower through `GROUP BY GROUPING SETS (...)` with each `grouping_set_list` entry emitted as a tuple (`(a, b)`), `ROLLUP (a, b)`, or `CUBE (a, b)`; GROUPING() calls land as `GROUPING(<col>) AS "<output>"` projections. |
| `ResolvedAnonymizedAggregateScan`             | `unsupported`         | Differential-privacy variant; `docs/ENGINE_POLICY.md` records the deliberate posture.                              |
| `ResolvedDifferentialPrivacyAggregateScan`    | `unsupported`         | Same DP family; `docs/ENGINE_POLICY.md`.                                                                           |
| `ResolvedAggregationThresholdAggregateScan`   | `unsupported`         | Same DP family; `docs/ENGINE_POLICY.md`.                                                                           |
| `ResolvedSetOperationScan`                    | `duckdb_native` (subset) | `transpiler-setops-sample`: `UNION ALL` / `UNION DISTINCT` / `INTERSECT DISTINCT` / `EXCEPT DISTINCT` (plus DuckDB-native `INTERSECT ALL` / `EXCEPT ALL` whose bag semantics match the GoogleSQL `*_ALL` shapes) all lower to the matching DuckDB keyword joined between the per-item SELECTs. Each `ResolvedSetOperationItem` projects its `output_column_list(i)` onto the parent's `column_list(i)` name. `column_match_mode=CORRESPONDING` / `CORRESPONDING_BY` use the same per-item projection once the analyzer maps columns by name. Pinned by `conformance/fixtures/setops/set_op_corresponding_union_all.yaml`. |
| `ResolvedOrderByScan`                         | `duckdb_native` (subset) | `transpiler-emit-join-agg`: `ORDER BY <col> [ASC|DESC] [NULLS FIRST|LAST]` per item. Clears analytic-captured `output_order_items_` so an explicit query `ORDER BY` is not overridden by window PARTITION/ORDER keys. `COLLATE` promotes to `semantic_executor` (`VisitResolvedOrderByScan`); `und:ci` sorts on `scan_eval_scan_impl.cc`. Pinned by `conformance/fixtures/scalar/order_by_collate_und_ci.yaml`. |
| `ResolvedLimitOffsetScan`                     | `duckdb_native`       | `transpiler-emit-join-agg`: `LIMIT <n> [OFFSET <m>]` via the literal-emit subset.          |
| `ResolvedAnalyticScan`                        | `duckdb_native` (subset) | `transpiler-functions-window`: emits `SELECT *, <fn> OVER (PARTITION BY ... ORDER BY ... [ROWS\|RANGE BETWEEN ... AND ...]) AS "<col>", ... FROM (<input>)`. ROW_NUMBER / RANK / DENSE_RANK / SUM/COUNT/AVG/MIN/MAX OVER (...) lower through the YAML disposition table. Hint lists, collation lists, lateral `parameter_list`, and `RANGE` over non-numeric ORDER BY reroute to `semantic_executor` (planned). |
| `ResolvedSampleScan`                          | `duckdb_native` (subset) | `transpiler-setops-sample`: lowers `TABLESAMPLE` to DuckDB `SELECT * FROM (<input>) USING SAMPLE <size> {PERCENT\|ROWS} (<method>)` for the method/unit matrix whose semantics match (`system` / `bernoulli` over PERCENT, `reservoir` over ROWS). Repeatable seeds, weight columns, stratified partition lists, and unsupported method/unit combos reroute to `duckdb_rewrite` (planned) via `docs/ENGINE_POLICY.md`. |
| `ResolvedWithScan`                            | `duckdb_native`       | `cte-subquery-routing`: non-recursive CTEs lower to DuckDB `WITH "a" AS (SELECT <inner> AS "_cte_<idx>" ...), ... <body>`. The CTE body projects each column to a positional anchor (`_cte_<idx>`) so per-reference `ResolvedWithRefScan` rename collisions cannot leak across the body. Recursive CTEs (`recursive==true`) bail to "" and the engine surfaces UNIMPLEMENTED until `docs/ENGINE_POLICY.md` lands its dedicated route. |
| `ResolvedWithRefScan`                         | `duckdb_native`       | `cte-subquery-routing`: emits `SELECT "_cte_<idx>" AS "<ref_col_name>" ... FROM "<cte_name>"`, renaming the WithScan-side positional anchors back to the per-reference column names the analyzer assigned. |
| `ResolvedRecursiveScan`                       | `duckdb_rewrite`      | `advanced-relational-routing`: `WITH RECURSIVE` lowers to DuckDB's `WITH RECURSIVE`. `EmitWithScan` pushes a per-CTE context (CTE name + stable anchor column names like `_cte_0`) before emitting the recursive scan; `EmitRecursiveScan` renames each arm's output columns onto the anchor names and unions them with `UNION ALL` / `UNION` per `op_type()`. `WITH DEPTH` (recursion_depth_modifier) still surfaces UNIMPLEMENTED.                                                                |
| `ResolvedRecursiveRefScan`                    | `duckdb_rewrite`      | `advanced-relational-routing`: the recursive-arm reference to the enclosing CTE. `EmitRecursiveRefScan` reads the back of `recursive_cte_stack_` for the active CTE name + anchor names and renames the anchors back to the per-ref column names so the surrounding scans compose like every other scan emit.                                                                |
| `ResolvedPivotScan`                           | `duckdb_rewrite`      | `advanced-relational-routing`: the engine disables `REWRITE_PIVOT` so the analyzer hands the transpiler a raw `ResolvedPivotScan`. `EmitPivotScan` lowers each `ResolvedPivotColumn` (an `(pivot_expr_index, pivot_value_index)` tuple plus the analyzer-chosen output column name) to a `<agg> FILTER (WHERE <for_expr> = <pivot_value>)` projection over the input scan, with `group_by_list` passing through. |
| `ResolvedUnpivotScan`                         | `duckdb_rewrite`      | `advanced-relational-routing`: the engine disables `REWRITE_UNPIVOT` so the analyzer hands the transpiler a raw `ResolvedUnpivotScan`. `EmitUnpivotScan` lowers to one UNION ALL branch per `unpivot_arg_list` entry, each branch projecting the input's pass-through columns, renaming the arg's column refs to `value_column_list`, and adding the `label_list[i]` literal under `label_column`. `EXCLUDE NULLS` (the BigQuery default) emits `WHERE NOT (val IS NULL AND ...)` per branch.                                                                                     |
| `ResolvedMatchRecognizeScan`                  | `semantic_executor`   | (planned) MATCH_RECOGNIZE is BigQuery-only and pattern-matching-heavy; `docs/ENGINE_POLICY.md`.                          |
| `ResolvedGroupRowsScan`                       | `semantic_executor`   | `advanced-relational-routing`: `WITH GROUP ROWS` clause (the special TVF `GROUP_ROWS()` used inside `ResolvedNonScalarFunctionCallBase::with_group_rows_subquery`). The classifier promotes any query containing one to the semantic executor; the executor surfaces a focused `kNotImplemented` naming the deferred follow-up until the per-group row-set evaluator lands. |
| `ResolvedTVFScan`                             | `semantic_executor`   | SQL TVF bodies evaluate via `MaterializeTvfScan` / `EvalSqlTvfBody`.                                    |
| `ResolvedRelationArgumentScan`                | `semantic_executor`   | (planned) Module / function arguments; `docs/ENGINE_POLICY.md`.                               |
| `ResolvedBarrierScan`                         | `semantic_executor`   | `advanced-relational-routing`: pipe-operator-only optimizer barrier. The classifier promotes any query containing a `ResolvedBarrierScan` to the semantic executor (the barrier exists specifically to block cross-barrier fusion that the DuckDB fast path cannot honor). The executor strips the barrier transparently in `StripBarrierScans` and dispatches on the wrapped scan (today `SingleRowScan` / `ArrayScan`; richer shapes follow downstream work).                      |
| `ResolvedPipeIfScan`                          | `semantic_executor`   | `advanced-relational-routing`: conditional pipe branching (`|> IF cond THEN ...`). The classifier promotes any query containing one to the semantic executor; the executor surfaces a focused `kNotImplemented` naming the deferred follow-up until the subpipeline evaluator lands. |
| `ResolvedPipeForkScan`                        | `semantic_executor`   | `advanced-relational-routing`: pipe `|> FORK` (terminates the main pipeline; each subpipeline runs over the input). Same `kNotImplemented` posture as `ResolvedPipeIfScan` until the subpipeline evaluator lands.                                     |
| `ResolvedPipeTeeScan`                         | `semantic_executor`   | `advanced-relational-routing`: pipe `|> TEE` (runs subpipelines as side-effects, then returns the input). Same `kNotImplemented` posture as `ResolvedPipeIfScan` until the subpipeline evaluator lands.                                     |
| `ResolvedPipeExportDataScan`                  | `control_op`          | `advanced-relational-routing`: pipe-operator `EXPORT DATA`. Pre-dispatched from the coordinator to `pipe_export_data.cc`, delegating to `RunExportData` on the inner `ResolvedExportDataStmt`. |
| `ResolvedPipeCreateTableScan`                 | `control_op`          | `advanced-relational-routing`: pipe-operator `CREATE TABLE`. Same wiring posture as `ResolvedPipeExportDataScan`: pre-dispatched from the coordinator to `backend/engine/control/pipe_create_table.cc`. The handler surfaces `kUnimplemented` naming the pipe-input transpiler surface as the deferred follow-up.                                     |
| `ResolvedPipeInsertScan`                      | `semantic_executor`   | (planned) INSERT pipe form routes through the local DML executor; `docs/ENGINE_POLICY.md`.                                     |
| `ResolvedGraph*Scan`                          | `unsupported`         | Graph query family (`GQL`); `docs/ENGINE_POLICY.md` records the deliberate posture.                                |

## Expressions

| Node                                          | Status                | Notes                                                                                     |
|-----------------------------------------------|-----------------------|-------------------------------------------------------------------------------------------|
| `ResolvedLiteral`                             | `duckdb_native`       | `transpiler-emit-scans` for scalars (via `Value::GetSQLLiteral(PRODUCT_EXTERNAL)`). `transpiler-struct-unnest` extends the path through a private `EmitValueLiteral` helper that recurses into `ARRAY` and `STRUCT` values so the nested STRING / STRUCT shapes use DuckDB-flavored quoting (`'...'`, `{'k': v}`) instead of BQ's `"..."` / `(...)`. `transpiler-json-struct-completion` finishes the anonymous-field subset by synthesizing positional names (`_0`, `_1`, ...). |
| `ResolvedParameter`                           | `duckdb_native`       | `transpiler-expression-core`: named (`@p`) and positional (`?`) parameters lower to DuckDB `$N` placeholders. The transpiler accumulates one `ParameterRef` per unique slot in `parameter_order()`. |
| `ResolvedColumnRef`                           | `duckdb_native`       | Emits the quoted `ResolvedColumn::name()` — `transpiler-emit-scans` plan.                  |
| `ResolvedFunctionCall`                        | `duckdb_native` (subset) | `transpiler-emit-scans` covers `COALESCE` + `IFNULL`; `transpiler-struct-unnest` adds `$make_array(<args>)`; `transpiler-functions-window` routes everything else through the YAML-backed disposition table (`functions.yaml` -> `functions_table.inc`). Math / string / array entries map directly. Datetime / regex / format functions on the `kFallback` disposition are slated to move to `duckdb_udf` (`docs/ENGINE_POLICY.md`) or `semantic_executor` (`docs/ENGINE_POLICY.md`) per family. BQ-specific (`APPROX_QUANTILES`, `ML.*`, `NET.*`, `HLL_COUNT.*`, `KEYS.*`, `ST_*`) live on `unsupported` via `docs/ENGINE_POLICY.md`. `SAFE.<fn>(...)` short-circuits regardless of disposition. |
| `ResolvedAggregateFunctionCall`               | `duckdb_native` (subset) | `transpiler-emit-join-agg` baseline + `transpiler-functions-window` disposition-table dispatch for `SUM`/`COUNT`/`AVG`/`MIN`/`MAX` + `COUNT(*)` (`$count_star`) plus YAML-table entries `ARRAY_AGG`, `STRING_AGG`, `ANY_VALUE`, `LOGICAL_AND`, `LOGICAL_OR`, `BIT_AND/OR/XOR`. Optional `DISTINCT` lowers as a prefix. `ARRAY_AGG` / `STRING_AGG` / `ARRAY_CONCAT_AGG` ORDER BY + LIMIT lower via `list(... ORDER BY ...)` + optional `list_slice(..., 1, n)` / `array_to_string`; `ARRAY_AGG` IGNORE NULLS lowers via `FILTER (WHERE ... IS NOT NULL)` and RESPECT NULLS via the `WrapArrayAggRespectNulls` error guard. Aggregate filtering (`WHERE` inside the call) lowers as DuckDB `FILTER (WHERE ...)`. `SAFE.<agg>(...)` (including `SAFE.SUM`) reroutes to `semantic_executor` (DuckDB rejects aggregates inside `TRY`). HAVING MAX/MIN and multi-level GROUP BY reroute to `semantic_executor`. Skiplisted aggregates (`APPROX_QUANTILES`, `COUNTIF`, ...) live on `unsupported`. Registered SQL UDAFs (`SQLFunction` with `mode=AGGREGATE`) promote the query to `semantic_executor` and evaluate via `EvalSqlUdafBody`. |
| `ResolvedAnalyticFunctionCall`                | `duckdb_native` (subset) | `transpiler-functions-window`: ROW_NUMBER, RANK, DENSE_RANK, PERCENT_RANK, CUME_DIST, NTILE, LAG, LEAD, FIRST_VALUE, LAST_VALUE, NTH_VALUE, and aggregate-over-window dispatch through the YAML disposition table. The OVER clause is stitched on by `EmitAnalyticScan`. IGNORE/RESPECT NULLS on FIRST_VALUE/LAST_VALUE/NTH_VALUE lower natively; `PERCENTILE_DISC` RESPECT NULLS uses the dedicated coalesce rewrite. LAG/LEAD null-handling, other IGNORE/RESPECT NULLS shapes, and `SAFE.<fn>(...)` reroute to `semantic_executor`. |
| `ResolvedCast`                                | `duckdb_native` (subset) | `transpiler-expression-core`: `CAST(<expr> AS T)` -> `CAST(<expr> AS <duckdb-type>)` (and SAFE_CAST -> `TRY_CAST(...)`) for every `TypeKind` `IsCastTargetSupported` whitelists. `FORMAT` / `AT TIME ZONE` promote to `semantic_executor` (`VisitResolvedCast` + `eval_expr_cast.cc` via googlesql `CastFormat*` / `CastStringTo*`); extended-cast / type-modifier shapes still surface `UNIMPLEMENTED`. Casts whose target is `TYPE_GEOGRAPHY` / proto / enum / range / graph / measure / tokenlist live on `unsupported`. Pinned by `conformance/fixtures/scalar/cast_format_*.yaml` and `cast_timestamp_at_timezone.yaml`. |
| `ResolvedMakeStruct`                          | `duckdb_rewrite`      | `transpiler-struct-unnest`: named-field `STRUCT(<expr> AS <name>, ...)` lowers to DuckDB's `{'<name>': <value>, ...}` struct literal. `transpiler-json-struct-completion`: anonymous-field structs (`STRUCT(1, 2)`) synthesize positional names (`_0`, `_1`, ...) -- a deliberate structural rewrite of BigQuery's positional STRUCT semantics into DuckDB's name-keyed STRUCT model. |
| `ResolvedGetStructField`                      | `duckdb_rewrite`      | Paired with `ResolvedMakeStruct`: anonymous-field access (`s.f0`) lowers to `<expr>."_<field_idx>"` using the same synthesized name policy. `field_expr_is_positional` is user-intent only and does not affect the emit. |
| `ResolvedMakeProto`                           | `unsupported`         | BigQuery proto types; `docs/ENGINE_POLICY.md`.                                              |
| `ResolvedGetProtoField`                       | `unsupported`         | Same.                                                                                     |
| `ResolvedGetProtoOneof`                       | `unsupported`         | Same.                                                                                     |
| `ResolvedReplaceField`                        | `unsupported`         | Proto-only; `docs/ENGINE_POLICY.md`.                                                           |
| `ResolvedGetJsonField`                        | `duckdb_rewrite`      | `transpiler-json-struct-completion`: BigQuery `<json>.<field>` lowers to DuckDB's `(<json> -> '<field>')` when the resolved return type is JSON (the common case -- chained `<json>.a.b` composes as `((<json> -> 'a') -> 'b')`) and to `(<json> ->> '<field>')` when the analyzer types the result as a scalar. Deliberate operator choice keeps BQ scalar-vs-JSON return-type semantics explicit. |
| `ResolvedGetRowField`                         | `unsupported`         | MEASURE type; `docs/ENGINE_POLICY.md`.                                                         |
| `ResolvedFlatten`                             | `semantic_executor`   | (planned) BigQuery FLATTEN; `docs/ENGINE_POLICY.md`.                                          |
| `ResolvedFlattenedArg`                        | `semantic_executor`   | (planned) Same.                                                                                     |
| `ResolvedSubqueryExpr`                        | `duckdb_native` (subset) | `cte-subquery-routing`: non-correlated scalar / IN / EXISTS / ARRAY lower directly to DuckDB (`(<sub>)`, `<lhs> IN (<sub>)`, `EXISTS (<sub>)`, `ARRAY(<sub>)`). Correlated forms (non-empty `parameter_list()`) evaluate on `semantic_executor` via `EvalSubqueryExpr` + `outer_row_eval` (`conformance/fixtures/cte_subquery/subquery_expr_correlated_exists.yaml`). LIKE ANY/ALL stays on the empty-string fallback (deferred). |
| `ResolvedWithExpr`                            | `duckdb_native`       | `transpiler-expression-core`: `WITH(<a> AS <e1>, ...) <body>` -> `(SELECT <body> FROM (SELECT <e1> AS "a", ...))`. The inner SELECT exposes each binding under its `ResolvedColumn::name()`; the surrounding scalar subquery preserves once-per-row evaluation. |
| `ResolvedInlineLambda`                        | `semantic_executor`   | (planned) Lambda-arg used by a few BQ-specific function families; `docs/ENGINE_POLICY.md`.              |
| `ResolvedFilterField`                         | `unsupported`         | Proto-only; `docs/ENGINE_POLICY.md`.                                                           |
| `ResolvedFilterFieldArg`                      | `unsupported`         | Proto-only.                                                                               |
| `ResolvedConstant`                            | `duckdb_native`       | `transpiler-emit-expr`: literal constants (including analyzer-bound parameter literals) lower through `EmitLiteral` so aggregate/order-by plans stay on the DuckDB fast path; catalog-backed `CREATE CONSTANT` module registration remains deferred. |
| `ResolvedSystemVariable`                      | `semantic_executor`   | `@@time_zone` defaults to UTC via `system_variables.cc`; scripting `@@error.*` reads resolve through `EvalContext::script_system_variables` (populated by `googlesql::ScriptExecutor` during `EXCEPTION` handlers). Pinned by `conformance/fixtures/scripting/exception_error_message.yaml` and `exception_statement_text.yaml`. |
| `ResolvedSequence`                            | `unsupported`         | SEQUENCE objects; `docs/ENGINE_POLICY.md`.                                                  |
| `ResolvedArgumentRef` / `ResolvedArgumentDef` | `semantic_executor` (subset) | `udf-tvf-module-routing`: `ResolvedArgumentRef` resolves against `EvalContext::arguments` (a `semantic::FrameStack` the UDF / TVF caller pushes per invocation). Missing frame surfaces a structured `kInvalidArgument` naming the argument rather than substituting NULL. `ResolvedArgumentDef` signature metadata is serialized at routine persist time (`SerializeFunctionSignatureJson` in `routine_persistence.cc`) and recovered when stored DDL is re-analyzed at rehydrate. |
| `ResolvedCatalogColumnRef`                    | `control_op`          | (planned) Catalog-time column ref inside DDL option lists; no landed `ALTER TABLE` / `LOAD DATA` shape in plan 09 required an `EmitCatalogColumnRef` lowering — keep `UNIMPLEMENTED` until a conformance fixture pins a consumer. |
| `ResolvedExpressionColumn`                    | `unsupported`         | Expression-mode analyzer; emulator does not use it.                                       |
| `ResolvedUpdateConstructor`                   | `semantic_executor`   | (planned) DML expr; `docs/ENGINE_POLICY.md`.                                                       |

## Per-row metadata / shape helpers

| Node                                          | Status                | Notes                                                                                     |
|-----------------------------------------------|-----------------------|-------------------------------------------------------------------------------------------|
| `ResolvedOutputColumn`                        | `duckdb_native`       | `transpiler-select-core`: `<column-name> AS <output-name>` (alias collapsed when the names match). Used by `EmitQueryStmt` for the outermost projection. |
| `ResolvedComputedColumn`                      | `duckdb_native`       | `transpiler-select-core`: `<expr> AS "<resolved-column-name>"`; child expression failures propagate the empty-string fallback contract. |
| `ResolvedDeferredComputedColumn`              | `semantic_executor`   | `advanced-relational-routing`: side-effect-aware computed column (deferred error capture via `side_effect_column`). The route classifier promotes any query containing one to the semantic executor; the executor itself surfaces `UNIMPLEMENTED` until the deferred-evaluation semantics are wired up (downstream plan).                                                       |
| `ResolvedOrderByItem`                         | `duckdb_native` (subset) | `transpiler-emit-join-agg`: lowered inside `EmitOrderByScan`; emits `<col> ASC|DESC [NULLS FIRST|LAST]`. `COLLATE` promotes to `semantic_executor` with the parent `ResolvedOrderByScan`. |
| `ResolvedColumnHolder`                        | `semantic_executor`   | UNNEST WITH OFFSET shape; `backend/engine/semantic/array_struct/array_scan.cc` binds the holder's `ResolvedColumn` to the per-row INT64 offset; `docs/ENGINE_POLICY.md` Family 1. |
| `ResolvedGroupingSet*` / `ResolvedRollup` / `ResolvedCube` | `duckdb_rewrite` | `advanced-relational-routing`: GROUPING SETS / ROLLUP / CUBE lower onto DuckDB's `GROUP BY GROUPING SETS (...)` form via `EmitGroupingSetEntry` (see the `ResolvedAggregateScan` row).         |
| `ResolvedAggregateHavingModifier`             | `semantic_executor`   | HAVING MAX/MIN inside aggregate calls; `scan_eval_agg_having.cc` filters input rows before aggregate evaluation. Conformance: `conformance/fixtures/aggregate/aggregate_any_value_having_max.yaml`. |
| `ResolvedWindowFrame*` / `ResolvedAnalyticFunctionGroup` | `duckdb_native` (subset) | `transpiler-functions-window`: `ROWS|RANGE BETWEEN <bound> AND <bound>` via `EmitFrameBound`. Frame bounds with non-literal offsets the expr emit cannot lower reroute to `semantic_executor` (planned). |
| `ResolvedSetOperationItem`                    | `duckdb_native`       | `transpiler-setops-sample`: lowered inside the private `EmitSetOperationItem` helper. |
| `ResolvedFunctionArgument`                    | `duckdb_native` (subset) | `transpiler-expression-core`: routes a `generic_argument_list` slot through `EmitExpr(arg->expr())`. Non-expression slots (`scan` / `model` / `connection` / `descriptor` / `inline_lambda` / `sequence` / `graph`) reroute to `semantic_executor` (planned) or `unsupported` per family. |
| `ResolvedReturningClause`                     | `semantic_executor`   | `THEN RETURN` on INSERT/UPDATE/DELETE evaluates on `backend/engine/semantic/dml/dml_returning.cc`; rows stream before `dml_stats`. Pinned by `conformance/fixtures/dml/dml_returning.yaml`. |

## Coverage summary

* **Authoritative sources.** The tables above are a
  human-readable mirror; the machine-readable source of truth
  lives in two sibling YAML files in this directory:
  * `node_dispositions.yaml` — per-`ResolvedAST` node-kind
    disposition registry.     Each row carries
    `<NodeKind>: <disposition> [plan=<plan>] [status=planned]`
    using the canonical seven-route vocabulary from
    `backend/engine/disposition.h` (`duckdb_native`,
    `duckdb_rewrite`, `duckdb_udf`, `semantic_executor`,
    `control_op`, `local_stub`, `unsupported`). A Bazel `genrule`
    (`:node_dispositions_table_inc`) emits
    `node_dispositions_table.inc`, which `node_dispositions.cc`
    `#include`s inside a static
    `absl::flat_hash_map<std::string, NodeDispositionEntry>`.
    `LookupNodeDisposition` is the runtime entry point.
  * `functions.yaml` — per-BigQuery-function disposition registry.
    Same grammar plus an optional `duckdb_name=<NAME>` field for
    the `duckdb_native` / `duckdb_rewrite` rows that lower to a
    DuckDB function call. The Bazel `genrule`
    `:functions_table_inc` regenerates `functions_table.inc`;
    `LookupFunction` returns a `FnEntry` whose `disposition` field
    is the same `Disposition` enum the node table uses, so the
    router consumes one type for both halves of the registry.

  The `tools/check_disposition_parity` lint gate (runnable as
  `task lint:dispositions`; CI wires it into `task lint:run`)
  asserts that every row in `node_dispositions.yaml` agrees with
  the matching row in this markdown -- composite cells
  (`Foo / Bar`) and wildcards (`Foo*Bar`) are expanded both ways
  before comparison. If you edit either file and forget the other,
  the gate fails before the change reaches main.
* **Status today:** the DuckDB fast path is the only route with
  substantial production coverage. Landed wins include
  `ResolvedTableScan` / `ResolvedProjectScan` / `ResolvedFilterScan` /
  `ResolvedJoinScan` (INNER/LEFT/RIGHT/FULL/CROSS) /
  `ResolvedAggregateScan` (baseline) / `ResolvedOrderByScan` /
  `ResolvedLimitOffsetScan` / `ResolvedSetOperationScan` /
  `ResolvedAnalyticScan` (subset) / `ResolvedSampleScan` (subset) /
  STRUCT and ARRAY literals via `transpiler-struct-unnest` /
  anonymous-field structs via `transpiler-json-struct-completion` /
  the YAML-backed function disposition table /
  parameter binding via `ResolvedParameter` / Arrow-native output on
  the Storage Read API. The other routes (DuckDB rewrites + UDFs,
  semantic executor, control-op executor) land via the local-execution
  roadmap (`docs/ENGINE_POLICY.md`).
* **Function disposition table (`functions.yaml`).** The
  source-of-truth file ships ~140 BigQuery functions across math,
  string, datetime, conditional, array, aggregation, window, and
  BQ-specific categories. After
  `docs/ENGINE_POLICY.md`, every row carries one
  of the seven `Disposition` values from
  `backend/engine/disposition.h`:
  * `duckdb_native` (and the small reserved
    `duckdb_rewrite` lane) — emit
    `<entry.duckdb_name>(<args>)` directly. Covers the math /
    string / array / aggregation / window functions whose DuckDB
    counterparts already match BigQuery semantics.
  * `duckdb_udf` — the lowering is a DuckDB UDF/macro registered at
    engine startup (`backend/engine/duckdb/udf/registrar.cc`).
    Landed families: conditional (`bq_if`, `bq_isnull`), numeric
    (`bq_mod`, `bq_div`, `bq_log`), string (`bq_strpos`, `bq_split`),
    regex (`bq_regexp_contains`, `bq_regexp_replace`), datetime epoch
    wrappers (`bq_unix_*`, `bq_unix_date`). Each ready row carries
    `duckdb_name=<macro>` in `functions.yaml` and a matching fixture
    under `conformance/fixtures/functions/`. Interval-semantics
    datetime arithmetic (`DATE_ADD` month-end snap, calendar `*_DIFF` /
    `*_TRUNC`) and the extended `FORMAT_*` / `PARSE_*` families route
    through `semantic_executor` instead because googlesql owns the exact
    semantics.
  * `semantic_executor` (with `status=planned`) — runs on the
    local row/value executor because the BigQuery semantics are
    exact (not close): `SQRT_NUMERIC` (unreachable until signature-
    aware transpiler dispatch lands). Already ready (the
    `status=planned` marker has dropped): `BIT_COUNT`,
    `IEEE_DIVIDE`, `SAFE_DIVIDE`, `SAFE_NEGATE`, `SOUNDEX`,
    `INSTR`, `CONTAINS_SUBSTR`, `FORMAT_DATE`, `FORMAT_DATETIME`,
    `FORMAT_TIMESTAMP`, and the googlesql-backed `DATE_*` /
    `DATETIME_*` / `TIMESTAMP_*` add/sub/diff/trunc family.
  * `local_stub` — `docs/ENGINE_POLICY.md`'s
    deterministic-placeholder posture for client-library startup
    probes. Today: `KEYS.NEW_KEYSET`, `KEYS.KEYSET_LENGTH`. The
    `CREATE MODEL` statement-level analog lives in
    `node_dispositions.yaml::ResolvedCreateModelStmt`. Promoting
    a row off `local_stub` requires the matching real
    implementation, not a thicker stub.
  * `unsupported` — deliberately out of scope locally
    (`approx_quantiles`, `ml.*`, `keys.encrypt`,
    `keys.decrypt_bytes`, `st_*`, `generate_uuid`,
    `session_user`, `error`). Every row points at
    `docs/ENGINE_POLICY.md`. `BIT_COUNT` was on this
    list before `docs/ENGINE_POLICY.md` shipped a
    two's-complement-correct implementation in the semantic
    executor; `IEEE_DIVIDE` is also owned by the semantic
    executor for the IEEE 754 sentinel contract DuckDB's `/`
    cannot match. `HLL_COUNT.*` and `NET.*` graduated from
    `unsupported` to `semantic_executor status=planned` by the
    specialized-feature policy plan; the real implementations
    land via `docs/ENGINE_POLICY.md`.
* **DuckDB STRUCT / ARRAY quirks the fast path honors:**
  * **BigQuery STRUCT field order is positional**, but DuckDB STRUCTs
    are keyed by name. The transpiler walks the resolved `StructType`
    for names in parallel with `field_list` for values.
  * **DuckDB does not support anonymous STRUCT fields.** The
    transpiler synthesizes positional names (`_0`, `_1`, ...) on
    both the construction side (`EmitValueLiteral` /
    `EmitMakeStruct` emit `{'_0': v0, '_1': v1, ...}`) and the
    access side (`EmitGetStructField` resolves an empty
    `StructField::name` to `_<field_idx>`). This is a `duckdb_rewrite`,
    not a leak of DuckDB semantics into BigQuery.
  * **DuckDB `LIST` ↔ BigQuery `ARRAY`.** Both spell their literal as
    `[a, b, c]`, so the emit is a direct join. NULL element
    semantics also match (`NULL` propagates as the literal `NULL`).
  * **`NULL` in struct fields** lowers as the literal `NULL`, which
    DuckDB matches against BigQuery's "absent NULL field" semantics
    for struct literals.
  * **UNNEST + WITH OFFSET / lateral joins / multi-array zip / outer
    UNNEST** all reroute from the DuckDB fast path to
    `semantic_executor` via `docs/ENGINE_POLICY.md`.
    The semantic executor owns these shapes because their BigQuery
    evaluation order does not map cleanly onto DuckDB's
    `LATERAL` / `unnest(...)` model.
* **Next plan(s):**
  * `docs/ENGINE_POLICY.md` — install the explicit route
    classifier behind `Engine::Analyze` so the "always DuckDB fast
    path" default becomes an explicit, observable routing decision.
  * `docs/ENGINE_POLICY.md` — retire the
    `kMap` / `kFallback` / `kSkiplist` function-disposition enum in
    favor of the same six-route vocabulary used in this tracker.
  * `docs/ENGINE_POLICY.md` — tighten conformance
    on the shapes that already route to the DuckDB fast path.
* **Promotion policy.** Promoting a row to a non-`unsupported` route
  requires (a) an `Emit*` (or semantic-executor / control-op handler)
  that lowers / executes the shape cleanly, and (b) the conformance
  fixtures that exercise it (see the conformance-harness section of
  ROADMAP.md). `unsupported` rows stay UNIMPLEMENTED until a
  deliberate plan moves them; promoting one requires landing the
  matching plan from
  `docs/ENGINE_POLICY.md`.
