<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.example.bigquery</groupId>
  <artifactId>bigqueryconnection-snippets</artifactId>
  <packaging>jar</packaging>
  <name>Google Cloud BigQuery Connections Snippets</name>

  <!--
    The parent pom defines common style checks and testing strategies for our samples.
    Removing or replacing it should not affect the execution of the samples in anyway.
  -->
  <parent>
    <groupId>com.google.cloud.samples</groupId>
    <artifactId>shared-configuration</artifactId>
    <version>1.2.0</version>
  </parent>

  <properties>
    <maven.compiler.target>1.8</maven.compiler.target>
    <maven.compiler.source>1.8</maven.compiler.source>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!--
      bigquery-emulator live-IT lane: a curated subset of the upstream
      src/test/java/**/*IT.java suite runs against the local emulator's
      bigqueryconnection gRPC listener via maven-failsafe-plugin (configured
      below). Until the shallow Connection backend lands, these ITs fail
      with NOT_IMPLEMENTED-shaped errors; see
      third_party/README.md (Java section) for the verdict baseline.
    -->
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.google.cloud</groupId>
        <artifactId>libraries-bom</artifactId>
        <version>26.32.0</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.google.cloud</groupId>
      <artifactId>google-cloud-bigqueryconnection</artifactId>
    </dependency>

    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java-util</artifactId>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.google.truth</groupId>
      <artifactId>truth</artifactId>
      <version>1.4.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        bigquery-emulator live-IT lane: bind Failsafe to integration-test +
        verify. Restrict <includes> to the curated ITs targeted by the Java
        live-IT track (failing-IT inventory in
        .cursor/plans/java-its-task-conversion_a7b8c9d0.plan.md). System
        properties forward the emulator + storage gRPC endpoints;
        BqConnectionOpts reads them in the forked JVM.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.2.5</version>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <includes>
            <include>**/CreateAwsConnectionIT.java</include>
            <include>**/DeleteConnectionIT.java</include>
            <include>**/GetConnectionIT.java</include>
            <include>**/ShareConnectionIT.java</include>
            <include>**/UpdateConnectionIT.java</include>
          </includes>
          <systemPropertyVariables>
            <BIGQUERY_EMULATOR_HOST>${env.BIGQUERY_EMULATOR_HOST}</BIGQUERY_EMULATOR_HOST>
            <BIGQUERY_STORAGE_GRPC_ENDPOINT>${env.BIGQUERY_STORAGE_GRPC_ENDPOINT}</BIGQUERY_STORAGE_GRPC_ENDPOINT>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
