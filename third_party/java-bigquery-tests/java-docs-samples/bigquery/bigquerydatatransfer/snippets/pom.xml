<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.example.bigquery</groupId>
  <artifactId>bigquerydatatransfer-snippets</artifactId>
  <packaging>jar</packaging>
  <name>Google Cloud BigQuery Data Transfer Service Snippets</name>

  <!--
    The parent pom defines common style checks and testing strategies for our samples.
    Removing or replacing it should not affect the execution of the samples in anyway.
  -->
  <parent>
    <groupId>com.google.cloud.samples</groupId>
    <artifactId>shared-configuration</artifactId>
    <version>1.2.0</version>
  </parent>

  <properties>
    <maven.compiler.target>1.8</maven.compiler.target>
    <maven.compiler.source>1.8</maven.compiler.source>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!--
      bigquery-emulator live-IT lane: a curated subset of the upstream
      src/test/java/**/*IT.java suite runs against the local emulator's
      bigquerydatatransfer gRPC listener via maven-failsafe-plugin (configured
      below). Until the shallow DataTransfer backend lands, these ITs fail
      with NOT_IMPLEMENTED-shaped errors; see
      third_party/README.md (Java section) for the verdict baseline.
    -->
  </properties>


  <!-- [START bigquerydatatransfer_install_with_bom] -->
  <!--
    bigquery-emulator live-IT lane: bump libraries-bom from upstream's
    26.32.0 to 26.73.0. The older BOM resolves google-cloud-core to
    2.32.0, whose ServiceOptions.getResolvedApiaryHost() unconditionally
    returns "https://bigquery.googleapis.com/" and ignores any custom
    host set via BigQueryOptions.Builder.setHost(). That makes the
    BqOpts-style "route through BIGQUERY_EMULATOR_HOST" pattern silently
    no-op, so Create*TransferIT.setUp dataset/table provisioning hits
    live BigQuery and 401s instead of the emulator. core 2.44.1+
    (shipped by libraries-bom 26.73.0 → core 2.62.2) added the early
    return that honors the configured host. Matches the BOM pinned by
    java-bigquery/samples/snippets/pom.xml.
  -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.google.cloud</groupId>
        <artifactId>libraries-bom</artifactId>
        <version>26.73.0</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.google.cloud</groupId>
      <artifactId>google-cloud-bigquerydatatransfer</artifactId>
    </dependency>
    <!-- [END bigquerydatatransfer_install_with_bom] -->

    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java-util</artifactId>
    </dependency>

    <!-- Test dependencies -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.google.truth</groupId>
      <artifactId>truth</artifactId>
      <version>1.4.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.google.cloud</groupId>
      <artifactId>google-cloud-bigquery</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.google.cloud</groupId>
      <artifactId>google-cloud-pubsub</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        bigquery-emulator live-IT lane: bind Failsafe to integration-test +
        verify. Restrict <includes> to the curated ITs targeted by the Java
        live-IT track (failing-IT inventory in
        .cursor/plans/java-its-task-conversion_a7b8c9d0.plan.md). System
        properties forward the emulator + storage gRPC endpoints;
        BqDataTransferOpts reads them in the forked JVM.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.2.5</version>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <includes>
            <include>**/CreateAmazonS3TransferIT.java</include>
            <include>**/DisableTransferConfigIT.java</include>
            <include>**/ReEnableTransferConfigIT.java</include>
            <!--
              Missing-tests follow-up additions (see
              .cursor/plans/java-its-missing-tests_c9d0e1f2.plan.md): smoke ITs
              for the 8 third-party connector Create*Transfer samples. Expected
              to fail with io.grpc.StatusRuntimeException: UNIMPLEMENTED from
              the gapic DataTransferService client until the gRPC-server
              follow-up lands the DTS gRPC bridge; committed today so Failsafe
              discovers them and the per-IT mapping carries the implementation
              target.
            -->
            <include>**/CreateAdManagerTransferIT.java</include>
            <include>**/CreateAdsTransferIT.java</include>
            <include>**/CreateCampaignmanagerTransferIT.java</include>
            <include>**/CreatePlayTransferIT.java</include>
            <include>**/CreateRedshiftTransferIT.java</include>
            <include>**/CreateTeradataTransferIT.java</include>
            <include>**/CreateYoutubeChannelTransferIT.java</include>
            <include>**/CreateYoutubeContentOwnerTransferIT.java</include>
          </includes>
          <systemPropertyVariables>
            <BIGQUERY_EMULATOR_HOST>${env.BIGQUERY_EMULATOR_HOST}</BIGQUERY_EMULATOR_HOST>
            <BIGQUERY_STORAGE_GRPC_ENDPOINT>${env.BIGQUERY_STORAGE_GRPC_ENDPOINT}</BIGQUERY_STORAGE_GRPC_ENDPOINT>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
